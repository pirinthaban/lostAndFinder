# Contributing to Lost & Found App

Thank you for your interest in contributing! 🎉

## Getting Started

1. **Fork the repository**
2. **Clone your fork:**
   ```bash
   git clone https://github.com/pirinthaban/lostAndFinder.git
   cd lostAndFinder
   ```

3. **Set up Flutter:**
   - Install Flutter SDK (3.38.5+)
   - Run `flutter pub get`

4. **Configure Firebase:**
   - Copy `.env.example` to `.env`
   - Add your Firebase credentials
   - Run `flutterfire configure` (optional)

5. **Run the app:**
   ```bash
   flutter run -d chrome  # Web
   flutter run -d windows  # Desktop
   ```

## How to Contribute

### Reporting Bugs
- Check if the issue already exists
- Provide clear reproduction steps
- Include Flutter version and OS

### Feature Requests
- Open an issue describing the feature
- Explain the use case
- Wait for maintainer approval before implementing

### Pull Requests
1. Create a feature branch: `git checkout -b feature/your-feature`
2. Make your changes
3. Test thoroughly
4. Commit with clear messages: `git commit -m "Add: feature description"`
5. Push: `git push origin feature/your-feature`
6. Open a Pull Request

## Code Style
- Follow Flutter/Dart conventions
- Run `flutter analyze` before committing
- Use meaningful variable names
- Add comments for complex logic

## Areas We Need Help
- [ ] UI/UX improvements
- [ ] Localization (Sinhala/Tamil translations)
- [ ] Testing (unit & integration tests)
- [ ] Documentation
- [ ] Accessibility features
- [ ] Performance optimization

## Questions?
Open an issue with the "question" label.

Thank you for contributing! 🙏
