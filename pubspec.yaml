name: lost_and_finder
description: Open-source Lost & Found Community App for Sri Lanka with smart matching, auto-blur privacy, and trust-driven ecosystem.
publish_to: 'none'
version: 1.0.4+5

# Repository & Issue Tracker
repository: https://github.com/pirinthaban/FindBack
issue_tracker: https://github.com/pirinthaban/FindBack/issues
homepage: https://github.com/pirinthaban/FindBack

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Core
  cupertino_icons: ^1.0.6

  # State Management
  flutter_riverpod: ^2.4.9

  # Firebase
  firebase_core: ^3.15.0
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.0.0
  google_sign_in: ^6.1.5

  # UI Components
  flutter_animate: ^4.3.0

  # Navigation
  go_router: ^12.1.3

  # Image Handling
  image_picker: ^1.0.5

  # HTTP Client
  http: ^1.1.0

  # OTP Input
  pinput: ^5.0.0
  intl: ^0.20.2
  shared_preferences: ^2.5.4

  # Version checking and URL launcher
  package_info_plus: ^8.1.2
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/images/app_icon.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/app_icon.png"

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
