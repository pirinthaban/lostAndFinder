/**
 * Send match notifications
 */

export async function sendMatchNotification(match: any): Promise<void> {
  // Implemented in onItemCreated.ts
  console.log('Sending match notification:', match);
}
